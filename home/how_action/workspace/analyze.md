# Workspace Analyze

Select all `reference_links` in Phase2 and pass only exact Workspace text Links plus one bounded intent. This action does not accept directories, expand scopes, or choose additional files in Phase3. If a reference budget failure is returned, reduce the Link set or first locate narrower evidence with `workspace.search_text` and `workspace.read`.
