name = "workspace"
description = "Workspace resource inspection and mutation actions."
selection_hint = "Use when the task needs to inspect, create, or modify workspace resources."

[runtime]
timeout_seconds = 30
parallel_policy = "allowed"
