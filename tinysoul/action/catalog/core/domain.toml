name = "core"
description = "Core conversation and control actions."
selection_hint = "Use when the task should answer, ask for clarification, or produce a final response."

[runtime]
timeout_seconds = 60
parallel_policy = "allowed"
