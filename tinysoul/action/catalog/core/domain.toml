name = "core"
description = "Core conversation, reasoning, and progressive context inspection."
selection_hint = "Use to answer or reason, or when compressed current/prior Turn facts must be inspected before continuing."

[runtime]
timeout_seconds = 60
parallel_policy = "allowed"
