name = "session"
description = "Completed prior-Turn conversation and Action history."
selection_hint = "Use when fixed Session Background is insufficient for an earlier ask, answer, Action outcome, or trace detail."

[runtime]
timeout_seconds = 5
parallel_policy = "serial"
