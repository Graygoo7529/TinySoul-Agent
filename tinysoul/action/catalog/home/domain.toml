name = "home"
description = "Agent Home knowledge, memory, and skill resource actions."
selection_hint = "Use to read progressive resources or edit the active Agent Home overlay."

[runtime]
timeout_seconds = 30
parallel_policy = "serial"
