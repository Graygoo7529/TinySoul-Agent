name = "execution"
description = "Local script authoring and supervised command or code execution against a transactional Workspace mirror."
selection_hint = "Use when deterministic local code or an immediate command should process Workspace resources or run as a bounded longer task."

[runtime]
timeout_seconds = 70
parallel_policy = "serial"
