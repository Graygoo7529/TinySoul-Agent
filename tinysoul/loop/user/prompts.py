"""User Turn prompt guidance."""

USER_TURN_GUIDANCE = (
    "Continue the current user request using the available Context and actions.",
    "Finish only by calling core.answer with the response intended for the user.",
)
