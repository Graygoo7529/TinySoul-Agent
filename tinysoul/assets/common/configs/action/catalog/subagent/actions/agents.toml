name = "subagent.agents"
domain = "subagent"

[tool]
description = "List configured external Agent targets."

[tool.schema]
type = "object"
required = []
additionalProperties = false

[semantic]
use_when = ["List configured external Agent targets."]
effects = ["read_only"]

[backend]
kind = "native"
handler = "subagent.agents"
