name = "core"
description = "Turn-level reasoning, conversation completion, progressive inspection of context and session history, and source-backed conversation organization."
selection_hint = "Use when the next step is to inspect internal state, preserve useful current memory, reason, or answer."

[visibility]
default = true

[runtime]
timeout_seconds = 60
parallel_policy = "allowed"
