name = "memory"
description = "Persistent daily and knowledge Memory actions."
selection_hint = "Use to search, inspect, memorize, and maintain Memory owned documents."

[visibility]
default = true

[runtime]
timeout_seconds = 60
parallel_policy = "serial"
