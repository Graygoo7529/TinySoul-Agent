# Workspace Append

Append the exact text fragment required by the current plan to an existing
Workspace text resource. Include separators or newlines explicitly. Use the
latest digest when a stale edit would be unsafe. Choose `workspace.patch` when
the intent is to replace a precise existing span rather than add content.
