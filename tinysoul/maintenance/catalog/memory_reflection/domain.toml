name = "memory_reflection"
description = "Maintain durable Memory through validated single-document writes."
selection_hint = "Use after inspecting relevant facts and existing content."

[runtime]
enabled = true
timeout_seconds = 60
parallel_policy = "serial"
