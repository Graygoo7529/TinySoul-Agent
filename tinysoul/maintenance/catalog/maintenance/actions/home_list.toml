name = "maintenance.home.list"
domain = "maintenance"

[tool]
description = "List unresolved token-bound runtime changes and HOW reviews in the current Home Maintenance task."
schema = { type = "object", properties = {}, required = [], additionalProperties = false }

[semantic]
use_when = ["The current Home Maintenance task needs its unresolved review catalog"]
avoid_when = ["The current task is Memory Maintenance"]
effects = ["read_only"]

[backend]
kind = "native"
handler = "maintenance.home.list"
