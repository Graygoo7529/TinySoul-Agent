name = "maintenance.home.accept"
domain = "maintenance"

[tool]
description = "Accept one inspected runtime Home change into actual Home using its current review token."

[tool.schema]
type = "object"
required = ["token"]
additionalProperties = false

[tool.schema.properties.token]
type = "string"
description = "Current change review token returned by maintenance.home.list."

[semantic]
use_when = ["The reviewed runtime change should become actual Home content"]
avoid_when = ["The item is a HOW review, the token is stale, or the change has not been inspected"]
effects = ["modifying"]

[backend]
kind = "native"
handler = "maintenance.home.accept"
